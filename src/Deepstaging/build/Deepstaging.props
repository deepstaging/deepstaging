<?xml version="1.0" encoding="utf-8"?>
<!--
    This file is automatically imported by projects that reference the Deepstaging package.
    
    Note: LanguageExt.Core is declared as a package dependency so it's automatically
    included during restore. This props file can be extended for other build-time
    configurations if needed.
-->
<Project>
</Project>
